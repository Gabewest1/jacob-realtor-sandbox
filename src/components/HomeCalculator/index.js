import React from "react"
import styled from "styled-components"

export default class HomeCalculator extends React.Component {
    render() {
        return (
            <HomeCalculatorView>

            </HomeCalculatorView>
        )
    }
}

const HomeCalculatorView = styled.div`

`